# 연구 기여도 및 신규 실험 실행가능성 재검증

작성일: 2026-08-06 / 근거 문서: `EXPERIMENT_PLAN.md`(13~22장), `SNAPDRAGON_FEASIBILITY.md`, `RELATED_PAPERS.md`

## 0. 이 문서의 목적

이번 세션에서 쌓아온 새 아이디어들(Galaxy AI 경합, 적응형 디스패처, 다중모델 동시실행, CNN/VLM 확장 등)을 **논문으로 낼 때의 기여(contribution) 관점**에서 정리하고, 각 아이디어가 실제로 실행 가능한지 처음부터 다시 감사(audit)한다.

**중요한 전제**: 이 세션 안에서만 "문서상 가능해 보였던 것"이 실기 확인 단계에서 다르게 나온 사례가 세 번 있었다 — ① 유튜브 배경부하가 실제로는 GPU 연산을 안 뺏음(2-4), ② 오브젝트 지우개가 온디바이스에서 클라우드로 전환된 정황(18-1), ③ 노트 번역이 클라우드 허용 상태에서도 언어팩을 요구(18-3, 가설 기각). 그러므로 이 문서의 모든 "가능" 판정은 **"실기 확인 전"**이라는 딱지가 붙어 있다고 보고 읽을 것.

---

## 1. 선행연구 대비 우리 연구의 기여(Contribution)

### 1-1. vs "LLM Inference at the Edge"(arXiv 2603.23640, RELATED_PAPERS 그룹1 1위)

| 측면 | 이 논문 | 우리 프로젝트 |
|---|---|---|
| 컴퓨트 유닛 | Adreno GPU만(S24U에서 NPU는 명시적으로 제외) | NPU(hybrid/pinned)/GPU/CPU/qairt 전부 비교 |
| 경쟁 상대 | 없음(단일 워크로드만 지속 실행) | Samsung 자사 Galaxy AI 기능과의 실사용 경합(RQ7) |
| 기기 범위 | 삼성 1대(S24 Ultra)뿐 | 동일 세대·다른 폼팩터 3대(S22/Fold4/Flip4) + S24U/S26U |
| 모델 | LLM 1개 고정 | LLM+CNN+VLM 다양화(20장) |
| 결과물 성격 | 관찰/특성화 | 문제 규명 + 완화책 시도(14장 디스패처) |

우리 실험 프로토콜(EXPERIMENT_PLAN.md 5장)은 이 논문 방법론을 그대로 채택했으므로, **방법론 근거로 인용**하고 S24 Ultra 재현은 "측정 파이프라인 검증(sanity check)" 용도로만 방법론 절에 짧게 배치할 것 — 결과 절의 중심이 되면 안 됨(선행 대화에서 이미 확정한 원칙).

### 1-2. vs Puzzle(arXiv 2508.17764)

Puzzle은 **하나의 모델을 서브그래프로 쪼개** 유전 알고리즘으로 이기종 프로세서(CPU/GPU/NPU)에 자동 배치하는 시스템. 우리 M축(20-2)은 이보다 훨씬 단순(수동 배치, 자동 탐색 없음)하지만, Puzzle이 다루지 않는 지점 — **삼성 자사 1st-party AI 프로세스와의 경합**(RQ7) — 을 다룬다. Puzzle은 "우리가 만든 여러 모델끼리"의 경합이고, 우리 RQ7은 "우리 모델 vs 이미 기기에 깔린 삼성 앱"의 경합이라는 점에서 문제 설정 자체가 다름.

### 1-3. vs ShadowNPU(arXiv 2508.16703)

ShadowNPU는 "NPU에서 안 나가는(회피)" 전략(희소 어텐션으로 CPU/GPU 폴백 자체를 없앰). 우리 14장 디스패처는 "필요하면 NPU에서 GPU/CPU로 나간다"는 정반대 철학. 같은 문제(자원경합)에 대한 대조적 해법으로 관련연구 절에 나란히 인용 가능.

---

## 2. 신규 실험 항목별 실행가능성 재검증

### 2-1. RQ7 / BLG·Nx(Galaxy AI 동시사용 배경부하) — **불확실, 리스크 높음**

- 확인된 것: 오브젝트 지우개(BLG1) → One UI 8.5에서 클라우드 전환 의심(18-1, 커뮤니티 다수 보고). 노트 번역 → 토글과 무관하게 언어팩 항상 요구, 온디바이스 여부 자체 미확정(18-3, 사용자 실기로 가설 기각됨).
- 미검증 후보: Document Scan(BLG2), Interpreter(BLG3) — **다음 세션 최우선 확인 항목**.
- 안전판: 전부 실패하면 RQ7을 "Galaxy AI vs 우리"에서 "우리 모델끼리"(2-2, M축)로 스코프를 낮출 수 있음.

### 2-2. M축(우리 측 모델 동시실행) — **원리적으로 확실, 실기 미검증**

- 근거: `SNAPDRAGON_FEASIBILITY.md` 2-2(QAIRT 공식문서) — 다중 그래프 동시 실행 자체는 전 기기 가능, "Concurrent Resource Sharing" 최적화만 V81(S26 Ultra) 한정.
- 리스크: 구형 기기(v69)에서 "최적화 없이 되긴 되는" 수준이 실제로 안정적으로 동작하는지는 미확인.

### 2-3. 적응형 디스패처(14~15장) — **엔지니어링 부담 큼, 프레임워크 미확정**

- GenieX: 세션 재로드 비용 미확인(15-2) → 요청 단위 전환이 예상보다 무거울 수 있음.
- llama.cpp CLI / ExecuTorch(16, 21장): 대안으로 지목했으나 아직 실기 설치·구동 테스트 시작 전.
- 권고: 정식 매트릭스가 아니라 "시간 남으면" 선택 확장으로 유지(14-3에서 이미 명시한 원칙 재확인).

### 2-4. CNN/VLM 모델 다양성(20장)

- **CNN(ResNet/YOLOv8): 상태 양호** — Qualcomm AI Hub 공식 문서로 HTP v69~v81 전체 지원 확인됨(20-1).
- **VLM: (2026-08-06 갱신) 불확실성 일부 해소** — GenieX가 LLM과 **동일한 Android SDK(4계층: Kotlin API-JNI-네이티브)**로 VLM도 공식 지원함을 확인. 21장에서 "GenieX와 별도 경로로 추정"이라 적어뒀던 부분은 정정. 다만 8 Elite+ 요구사항이 VLM에도 동일 적용되는지는 여전히 실기 확인 필요.
- **참고(신규 발견)**: Nexa SDK(`github.com/qualcomm/nexa-sdk`)라는 대안도 발견 — GPU/NPU/CPU 걸쳐 LLM+VLM 지원, Qwen3-VL 등 최신 모델 day-0 지원 표방. GenieX/ExecuTorch에 이은 세 번째 프레임워크 후보로 21장 표에 추가할 만함(미검증).

### 2-5. 폼팩터 비교(S22/Fold4/Flip4) — **양호**

CNN 경로(20-1)를 쓰면 GenieX 없이도 3대 전부에서 실행 가능 — 9-6에서 이미 원리 확인됨(HTP v69 공식 지원).

### 2-6. 프레임워크 독립성(21장) — **원칙만 확정, 실물 설치·테스트 미시작**

지금까지 전부 "가능하다는 근거 문서/커뮤니티 확인" 단계이지, 실제로 폰에 설치해본 적은 아직 없음. **이 문서 전체에서 가장 근본적인 리스크는 "문서상 가능 ≠ 우리 기기에서 실제로 됨"이라는 것** — 이번 세션 내내(유튜브, 오브젝트 지우개, 노트 번역) 반복 확인된 패턴.

### 2-7. 기기 로스터 갱신

S24 Ultra 추가 확인(SM8650, Snapdragon 8 Gen 3, HTP v75) — RELATED_PAPERS.md 그룹1 논문 다수(PowerInfer-mobile, ShadowNPU, OnePlus 13R 사례연구)와 칩 세대가 일치해 재현 실험에 유리. **PROGRESS_LOG.md/EXPERIMENT_PLAN.md의 기기 목록에는 아직 미반영 — 별도 갱신 필요.**

---

## 3. 종합 — 다음 세션 우선순위(리스크 높은 순)

1. Document Scan/Interpreter가 실제로 온디바이스인지(2-1) — RQ7 생존 여부가 걸린 최우선 항목.
2. llama.cpp Hexagon CLI 또는 ExecuTorch 실기 설치·구동 테스트(2-3, 2-6) — 지금까지 전부 "문서상 가능"만 확인, 실물 설치는 0건.
3. GenieX VLM이 실제로 8 Elite+ 제약을 받는지, S26 Ultra에서 정말 구동되는지(2-4).
4. PROGRESS_LOG.md/EXPERIMENT_PLAN.md 기기 목록에 S24 Ultra 반영.

## 4. 이 문서의 한계

이 문서의 "가능" 판정 대부분은 공식 문서·커뮤니티 검색 기반이며, 실기 검증 완료는 거의 없다. 이번 세션에서만 세 번(유튜브, 오브젝트 지우개, 노트 번역) 문서상 가능해 보였던 것이 실기에서 다르게 나온 전례가 있으므로, 이 문서의 모든 계획은 **"실기 확인 전"**이라는 전제 하에 읽을 것.
