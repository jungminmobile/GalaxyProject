# 연구 설계 요약 (Study Design)

작성일: 2026-08-06 / 목적: 외부 검토(다른 AI 포함)를 위해 지금까지의 논의를 하나로 정리한 최종 요약본. **히스토리(왜 이렇게 바뀌었는지)는 `EXPERIMENT_PLAN.md`에 있고, 이 문서는 현재 확정된 계획만 담는다.**

---

## 1. 연구 배경

삼성 갤럭시 스마트폰(스냅드래곤 기반)에서 온디바이스 AI(LLM 등)를 반복 실행할 때,

1. 어떤 조건(NPU/GPU/CPU, 기기, 폼팩터)에서 성능이 얼마나 유지·저하되는지
2. 삼성 자사 AI 기능(Galaxy AI)이 동시에 돌아갈 때 제3자 온디바이스 AI 성능이 어떻게 영향받는지, 그 영향이 공정한지(OS/NPU 스케줄러가 자사 프로세스를 우대하는지)
3. 삼성이 광고하는 세대 간 성능 개선("NPU 39% 향상, 멀티태스킹 문제없음")이 실사용 제약 조건에서도 유지되는지

를 실측한다.

## 2. 선행연구와의 관계

가장 가까운 선행연구는 **"LLM Inference at the Edge: Mobile, NPU, and GPU Performance Efficiency Trade-offs Under Sustained Load"**(arXiv 2603.23640, 2026)로, 4개 플랫폼(노트북 GPU / RPi5+Hailo-10H / iPhone 16 Pro / Galaxy S24 Ultra)에서 동일한 프로토콜(지속부하 20회 반복)로 플랫폼별 성능 열화 패턴을 비교함.

우리 실험은 이 논문의 프로토콜(10분 열안정화 → 워밍업 폐기 → 20회 반복 → 3세트)을 그대로 채택하되, 다음을 확장한다:

- 삼성 기기를 1대가 아니라 **5대 + 데스크탑**(추후 엣지가속기)으로 확장
- 컴퓨트 유닛을 GPU만이 아니라 **NPU/GPU/CPU**로 확장
- **삼성 자사 AI 기능과의 경합이라는, 이 논문을 포함해 조사한 어떤 선행연구에도 없는 축을 추가**(핵심 기여)

## 3. 실험 구조 — 2단계(Tier)

### Tier 1. 전체 플랫폼 비교 (넓게)

**목적**: 플랫폼마다 지속부하 시 성능 저하 "패턴의 종류" 자체가 다른지 확인(선행논문의 핵심 통찰 — 안드로이드는 완만한 하락, iOS는 계단식 급락 — 을 삼성 내부 다양성 + 새 플랫폼으로 확장 재현).

| 플랫폼 | 유형 | 확보 상태 |
|---|---|---|
| Galaxy S22 | 바형, 2022세대(Snapdragon 8 Gen1) | 보유 |
| Galaxy Z Fold4 | 폴더블, 2022세대(Snapdragon 8+ Gen1) | 보유 |
| Galaxy Z Flip4 | 클램쉘, 2022세대(Snapdragon 8+ Gen1) | 보유 |
| Galaxy S24 Ultra | 바형, 2024세대(Snapdragon 8 Gen3) | 보유 |
| Galaxy S26 Ultra | 바형, 2026세대 최신(Snapdragon 8 Elite Gen5) | 보유 |
| 데스크탑(RTX 4080) | 고정형, 전력·냉각 무제약 | 보유 |
| Jetson Orin Nano | 저전력 전용 엣지 가속기 | **선택, 추후** — 미보유, 시간 되면 추가 |

**방법**: 동일 모델(LLM 또는 CNN)을 각 플랫폼에서 단독으로 지속부하 프로토콜(아래 4장) 실행 후 회차별 성능 곡선 비교.

### Tier 2. 삼성 세대 비교 — 핵심 기여 (좁고 깊게)

**목적**: 삼성이 광고하는 세대 간 성능 개선이 이상적 조건뿐 아니라 **실사용 제약 조건에서도 유지되는지** 확인.

**대상**: Galaxy S24 Ultra vs Galaxy S26 Ultra(둘 다 8 Elite급 이상이라 Galaxy AI 온디바이스 스택이 유효하게 동작하는 유일한 조합).

| 조건 | 내용 | Tier 1 데이터 재사용 |
|---|---|---|
| A. 이상적 조건 | 단독 실행 | O(Tier 1과 동일 데이터) |
| B. 제한된 상황 1 | "기기에서만 처리" 토글 항상 ON + **통역(Interpreter)**(2026-08-07 S26U 실기 확인 완료 — 지속부하형 워크로드) 동시 실행 | X(신규 측정) |
| C. 제한된 상황 2 | 절전모드/성능제한모드 | X(신규 측정, 대표 기기 위주) |

세 조건을 S24U·S26U 둘 다에서 비교해 **"S26U의 성능 우위가 조건 B·C에서도 그대로 유지되는가, 줄어드는가"**를 본다 — 이 질문이 논문의 핵심 기여.

#### Tier 2 조건 B — Galaxy AI 워크로드 설계 세부사항(외부 검토 반영, 2026-08-06)

- **기능 선정 기준**: 아무 Galaxy AI 기능이나 시험해보지 않고, 설정 > Galaxy AI > "기기에서만 처리" 화면에 **공식적으로 목록에 등재된 기능 중에서만** 선택한다 — 18장에서 반복된 "알고 보니 클라우드였다"는 실패를 원천 차단.
- **지속 부하(sustained load) 우선**: 오브젝트 지우개 같은 단발성 기능(1~3초 완료 후 재트리거 필요)보다, **통역 앱·음성 받아쓰기(dictation)처럼 시작하면 계속 도는 지속성 기능**을 우선 고려한다 — 20회 반복 벤치마크 내내 별도 재트리거 자동화 없이 배경부하를 유지할 수 있어 13-5의 "반복 트리거 자동화 부담" 문제가 사라짐.
- **표준화된 입력**: 외부 스피커로 고정 길이 오디오(예: 3~5분 뉴스 음원)를 재생하거나 고정된 글자 수(예: 1,000단어) 텍스트를 입력해, 매 회차 동일한 부하량을 보장한다(폰 내장 스피커/마이크 루프백은 에코·피드백 위험이 있어 제외).

#### 핵심 설계 원칙 — OS 백그라운드 스로틀링 통제

- **화면 분할(split screen) 필수**: 서드파티 벤치마크 앱과 Galaxy AI 앱을 **둘 다 화면분할로 최상단(foreground)에 띄운 채** 실행한다. 안드로이드는 백그라운드 프로세스에 cgroups 기반 자원 제약을 강제로 걸기 때문에, 하나라도 백그라운드로 밀리면 측정치가 "경합 때문"이 아니라 "OS가 배경앱을 줄였기 때문"에 왜곡될 수 있다 — 2-4의 BL2(유튜브) 교훈과 동일한 원리를 Track B 전체의 표준 절차로 격상.
- **배터리 설정 '제한 없음'**: 서드파티 벤치마크 앱의 배터리 사용을 '제한 없음(Unrestricted)'으로 변경해, OS 레벨 배터리 최적화 스케줄러의 개입을 원천 차단한다. 이는 RQ7-2(공정성) 분석의 정확도에도 도움이 됨 — 이 설정을 해두면 남는 격차는 배터리 최적화 정책이 아니라 NPU/스케줄러 자체의 효과로 더 좁혀서 해석할 수 있다.

---

## 4. 실험 순서

> **⚠️ 용어 구분 — 이 문서에는 서로 다른 두 개의 축이 나온다. 혼동하지 말 것.**
>
> | 축 | 의미 | 값 |
> |---|---|---|
> | **Phase** | 실험을 **언제, 어떤 순서로** 하는가(실행 순서) | Phase 0(준비) → 1(검증) → 2(Tier1 측정) → 3(Tier2 측정) → 4(분석) |
> | **조건 A/B/C** | 같은 벤치마크를 **어떤 제약 상태에서** 도는가(3장 Tier 2 전용) | A(제약 없음) / B(Galaxy AI 동시실행) / C(절전모드) |
>
> 즉 아래 **Phase 2에서 조건 A(제약 없음) 데이터를 모으고**, **Phase 3에서 같은 벤치마크·같은 측정 항목을 조건 B·C(제한된 상황)로 다시 돌려서** 조건 A와 직접 비교한다. 조건 A/B/C는 셋 다 동일한 프로토콜·동일한 측정 항목을 쓰고, 오직 "제약이 있는가/없는가"만 다르다 — 그래야 차이가 나면 그게 온전히 그 제약 때문이라고 말할 수 있다.

**Phase 0. 환경 준비**
- 기기별 개발자모드/USB디버깅 확인(S26U만 완료, 나머지 4대 미확인)
- **프레임워크: llama.cpp를 메인 측정 도구로 채택**(2026-08-06 확정, 외부 검토 반영) — `--device`(`D=`) 옵션으로 CPU/GPU/NPU를 명시적으로 강제 지정하는 화이트박스 제어가 가능하고, tok/s·TTFT를 표준 출력으로 내보내 자동화 연동이 쉬움. GenieX(Developer Preview, 세션 동시로드 미확인·15-2)와 ExecuTorch(NDK 빌드 난이도 높아 일정 지연 리스크)는 백업/보조로 격하.
  - GPU 백엔드: **OpenCL(`GGML_OPENCL`, 공식명 GPUOpenCL)** — ⚠️ Vulkan 백엔드는 Adreno GPU에서 **모델 로드 자체가 실패(크래시)**하는 것으로 llama.cpp 공식 GitHub 이슈에 다수 보고됨. Qualcomm도 이 때문에 Adreno 전용 OpenCL 백엔드를 별도 공식 도입(Qualcomm 개발자 블로그, Snapdragon 8 Gen3/8 Elite·Adreno 750/830 지원 명시 — S24U/S26U 해당). **Vulkan이 아니라 반드시 OpenCL로 빌드할 것.**
  - NPU 백엔드: Hexagon(HTP0-4), `D=HTP0` 등으로 지정(10-A-1에서 이미 확인).
  - 빌드: `ghcr.io/snapdragon-toolchain/arm64-android` Docker 툴체인 이미지 사용(NDK·OpenCL SDK·Hexagon SDK 포함, 10-A-1).
- 데스크탑: llama.cpp CUDA 빌드(RTX 4080)
- 측정 환경: 통제된 실내 온도(에어컨 설정 기준, 논문 프로토콜의 22±2°C에 최대한 맞춤 — 정확히 25°C로 고정할 경우 논문 대비 소폭 벗어남을 한계로 명시)
- **자동화 파이프라인 구축(Python + ADB, 외부 검토 반영)**: 수동 측정을 배제하고 아래 4개 기능을 갖춘 스크립트를 Phase 2 시작 전 완성
  1. 배터리 온도 모니터링 기반 자동 쿨다운·워밍업 루프(열 안정화 자동 판정)
  2. 정규표현식 기반 실시간 프레임워크 로그 파싱(tok/s, TTFT)
  3. Snapdragon Profiler + `adb dumpsys battery`로 하드웨어 데이터(클럭·전력·온도) 실시간 덤프
  4. 회차별(1~20회) 추론 즉시 CSV 행 단위 저장(append)
- **지연시간 측정 방식**: 수동 스톱워치 대신 **60fps 이상 외부 카메라(또는 캡처보드)로 화면을 녹화**해 프레임 단위로 분석, ms 단위 응답시간 산출 — 5장 측정표에 반영

**Phase 1. 불확실성 우선 해소 — 데이터 수집 전 필수**
- Galaxy AI 후보 기능이 실제 온디바이스인지 확인(설정 > Galaxy AI > "기기에서만 처리" 목록에 공식 등재된 것만 후보로 삼음)
  - **(2026-08-07 확정) 통역(Interpreter) — 온디바이스 확인 완료**: 비행기모드에서 뉴스 영어 낭독 → 번역+TTS 변환까지 정상 동작을 S25 FE(Exynos 2400)에서 1차 확인 후, **이 프로젝트의 실제 대상 기기인 S26U(Snapdragon, Hexagon NPU)에서도 동일하게 정상 동작 재확인됨.** 오브젝트지우개·노트번역과 달리 처음으로 실기 테스트를 완전히 통과한 Galaxy AI 후보 — **Track B(RQ7) 조건 B의 워크로드로 통역 채택 확정.** S24U에서도 동일 결과가 나올 가능성이 높지만(같은 8 Elite+ 계열은 아니어도 Galaxy AI 온디바이스 스택 자체는 공유), 시간 되면 S24U도 한 번 더 확인하는 정도로 우선순위 하향.
- "기기에서만 처리" 토글의 정확한 적용 범위 확인
- 프레임워크 중 실기에서 먼저 되는 것 확인
- **NPU 실제 점유 검증(외부 검토 반영, 최우선)**: compute_unit="npu"로 설정해도 프레임워크가 실제로 Hexagon NPU를 쓰는지, 일부 연산이 CPU/GPU로 조용히 폴백되지 않는지 Snapdragon Profiler로 교차 검증 — API 파라미터만 믿지 않고 실측으로 확인
- **모델·입출력 고정**: 논문(arXiv 2603.23640)과 동일한 **Qwen2.5-1.5B(4bit)**을 채택(다른 모델로 바꾸면 직접 비교가 깨짐), 입력 프롬프트와 `max_new_tokens`를 고정해 매 회차 동일 연산량 보장, 구형 기기(S22/Flip4 등)에서 OOM 없이 로드되는지 사전 확인

**Phase 2. Tier 1 데이터 수집(전체 플랫폼) — = 조건 A(제약 없음) 데이터 수집**
- 온도 안정화 → 워밍업 폐기 → 20회 반복 × 3세트, 전 플랫폼 동일 프로토콜
- S24 Ultra·S26 Ultra는 여기서 수집한 데이터가 그대로 Tier 2의 **조건 A**가 됨(재측정 불필요)

**Phase 3. Tier 2 데이터 수집(S24U/S26U 세대비교) — = 조건 B·C(제한된 상황)만 신규 측정**
- Phase 1 확인이 끝난 뒤에만 진행(조건 B가 Phase 1 결과에 의존)
- 조건 A(Phase 2에서 이미 확보)와 **동일한 프로토콜·동일한 측정 항목**으로, Galaxy AI 동시실행(B)·절전모드(C) 상태에서만 새로 측정
- Phase 2 결과(조건 A)와 나란히 비교해 "제약 상황에서 성능이 얼마나/어떻게 달라지는가" 산출

**Phase 4. 분석 및 문서화**

---

## 5. 측정 항목(종속변인) 및 분석 방법

| 항목 | 측정 방법 | 비고 |
|---|---|---|
| 처리량(tok/s 또는 FPS) | 자체 타임스탬프 | 전 조건 공통 |
| 지연시간(TTFT, decode time) | 자체 타임스탬프 | 전 조건 공통 |
| 온도 | 배터리 온도 API / 자체 센서 | 전 조건 공통 |
| 전력(전류·전압) | Android BatteryManager API | 전 조건 공통 |
| GPU/NPU 클럭(MHz) | Snapdragon Profiler | **S24U·S26U만 공식 지원**(SM8650/SM8850), 나머지 3대는 미지원 |
| Galaxy AI 기능 자체 응답시간 | **60fps+ 외부 카메라/캡처보드로 화면 녹화 후 프레임 단위 분석**(ms 산출) | Tier 2 조건 B에서만. 자동 로그 접근이 안 되는 블랙박스 앱이라 이 방식 채택(수동 스톱워치보다 노이즈 훨씬 적음, 2026-08-06 방법 변경) |

**분석 절차**:

1. 회차별 처리량·온도 곡선을 그려 "몇 회차부터 안정화되는가", "피크 대비 몇 % 저하되는가" 산출(Tier 1·2 공통 포맷, 선행논문과 직접 비교 가능하도록 동일 포맷 사용).
2. Tier 1: 플랫폼 간 곡선 **형태**(완만/급락/거의 평평) 비교.
3. Tier 2: 조건 A/B/C별 S24U-S26U 격차 비교 — 격차가 좁혀지면 "제약 상황에서 삼성의 우위가 줄어든다"는 결론.
4. Tier 2-B: 우리 측정치와 Galaxy AI 기능 자체 응답시간의 저하율을 나란히 비교해 비대칭성(자사 프로세스 우대 여부) 확인.

---

## 6. 불확실한 부분 — 검토 시 특히 봐주셨으면 하는 지점

| 항목 | 리스크 수준 | 실패 시 대응 |
|---|---|---|
| Galaxy AI 후보 기능이 실제 온디바이스인지 | **해소(2026-08-07)** — 통역이 S26U(Snapdragon)에서 비행기모드+TTS 테스트 통과, Track B 조건 B 워크로드로 확정. S24U 재확인은 선택사항으로 하향 | 없음(리스크 해소) |
| S22/Fold4/Flip4/S24U 개발자모드 상태 | 중간 | 실기 확인 후 활성화, 기기관리(MDM) 제약 있으면 대체 필요 |
| GenieX/llama.cpp/ExecuTorch 중 무엇을 쓸지 | 중간 | 셋 다 "문서상 가능"만 확인됨, 실물 설치는 0건 |
| Snapdragon Profiler의 S24 Ultra 지원 여부 | 낮음(SM8650이 공식 지원 칩셋 목록에 있음) | 미지원으로 판명 시 스톱워치 기반 측정으로 대체 |
| Jetson Orin Nano 추가 여부 | 낮음(처음부터 선택사항으로 명시) | 시간 부족하면 생략, 데스크탑+5개 폰만으로도 Tier 1 결론 가능 |
| 데스크탑(4080)의 열화 곡선이 거의 안 나올 가능성 | 낮음(예상된 결과) | "모바일 칩만 이 정도로 스로틀링한다"는 대조 자체가 유효한 결과 |
| NPU 조건이 실제로 CPU/GPU로 조용히 폴백될 가능성 | 중간(2026-08-06 신규) | Phase 1에서 Snapdragon Profiler로 최우선 검증, 폴백 확인되면 해당 조건 결과 전체 재해석 필요 |
| 실내 온도를 정확히 25°C로 고정 시 논문(22±2°C)과 소폭 불일치 | 낮음(2026-08-06 신규) | 가능하면 22~24°C로 맞추고, 안 되면 한계로 명시 |
| Qwen2.5-1.5B가 구형 기기(S22/Flip4 등)에서 OOM 없이 로드되는지 | 낮음~중간(2026-08-06 신규) | Phase 1 사전 확인, 실패 시 더 낮은 양자화(Q4→더 낮은 비트) 검토 |
| 카메라/캡처보드 촬영 셋업(고정 거치, 화면 밝기·조명 통일) | 낮음(2026-08-06 신규) | 기기마다 동일 거치대·조명 조건 표준화 필요 |

---

## 7. 참고 문서

- `EXPERIMENT_PLAN.md` — 전체 설계 히스토리(왜 이렇게 결정됐는지, 1~23장)
- `CONTRIBUTION_FEASIBILITY.md` — 선행연구 대비 기여도 표, 항목별 리스크
- `EXPERIMENT_SEQUENCE.md` — Phase별 상세 체크리스트
- `RELATED_PAPERS.md` — 관련 논문 33편 목록(주제별 분류)
- `SNAPDRAGON_FEASIBILITY.md` — 기기·SDK별 세부 실행가능성 근거
